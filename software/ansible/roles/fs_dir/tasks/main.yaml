---
#  -*- mode:yaml; tab-width:2; intent-tabs-mode:nil; -*- ex: set filetype=yaml tabstop=2 expandtab:
## Create/remove directories

- name: "Create/remove directories"
  tags:
    - fs_dir
    - always
  include_tasks:
    file: "enable.yaml"
  when: FS_DIR_ENABLE|bool
