---
#  -*- mode:yaml; tab-width:2; intent-tabs-mode:nil; -*- ex: set filetype=yaml tabstop=2 expandtab:
## MQTT setup

- name: "MQTT setup"
  tags:
    - mqtt
    - always
  include_tasks:
    file: "{{ MQTT_ENABLE | bool | ternary('enable.yaml', 'disable.yaml') }}"
