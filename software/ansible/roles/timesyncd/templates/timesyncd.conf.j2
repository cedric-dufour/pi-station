## Generated by Ansible (timesyncd)

## Time Synchronization configuration
[Time]

# Server(s)
{% if TIMESYNCD_SERVERS|length > 0 %}
NTP={{ TIMESYNCD_SERVERS|join(' ') }}
{% endif %}
{% if TIMESYNCD_FALLBACK_SERVERS|length > 0 %}
FallbackNTP={{ TIMESYNCD_FALLBACK_SERVERS|join(' ') }}
{% else %}
#FallbackNTP=<built-in defaults>
{% endif %}
