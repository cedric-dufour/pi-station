---
#  -*- mode:yaml; tab-width:2; intent-tabs-mode:nil; -*- ex: set filetype=yaml tabstop=2 expandtab:
## NetworkManager setup: enable

- name: "Install NetworkManager packages"
  tags:
    - networkmanager
    - install
  apt:
    state: present
    name:
      - network-manager
