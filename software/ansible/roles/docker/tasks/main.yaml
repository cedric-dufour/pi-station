---
#  -*- mode:yaml; tab-width:2; intent-tabs-mode:nil; -*- ex: set filetype=yaml tabstop=2 expandtab:
## Docker (containers) setup

- name: "Docker (containers) setup"
  tags:
    - docker
    - always
  include_tasks:
    file: "{{ DOCKER_ENABLE | bool | ternary('enable.yaml', 'disable.yaml') }}"
