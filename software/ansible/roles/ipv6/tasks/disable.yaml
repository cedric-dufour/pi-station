---
#  -*- mode:yaml; tab-width:2; intent-tabs-mode:nil; -*- ex: set filetype=yaml tabstop=2 expandtab:
## IPv6 setup: disable

- name: "Disable IPv6"
  tags:
    - ipv6
    - configure
  copy:
    content: |
      ## Generated by Ansible (ipv6)

      # Disable IPv6
      net.ipv6.conf.all.disable_ipv6 = 1
    dest: "/etc/sysctl.d/05-ipv6-disable.conf"
    owner: "root"
    group: "root"
    mode: 0644
